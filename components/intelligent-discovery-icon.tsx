"use client"

export function IntelligentDiscoveryIcon({ active }: { active: boolean }) {
    return (
        <div className="w-full h-full flex items-center justify-center relative pointer-events-none">
            {/* Removed all graphics as requested */}
        </div>
    )
}
